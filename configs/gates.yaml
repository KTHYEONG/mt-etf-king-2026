gates:
  g1_prob_threshold: 0.30
  g1_min_improvement: 0.02
  g2a_ruin_threshold: -0.25
  g2a_max_prob: 0.05
  diagnostics:
  - cvar_05
  - mdd_distribution

  # deployment 모드에서만 전략 채택 판정 (structural 단독 채택 금지)
  require_universe_mode: deployment

championship:
  thresholds: [0.3, 0.4, 0.5, 0.6]
  primary_scenario: championship
  scenario_weights:
    weak_field: [0.55, 0.3, 0.1, 0.05]
    championship: [0.1, 0.25, 0.45, 0.2]
    hot_field: [0.0, 0.1, 0.35, 0.55]
  bootstrap_expected_block: 36
  bootstrap_resamples: 2000
  seed: 20260831
  min_era_effective: 5
